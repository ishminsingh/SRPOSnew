<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="dev.ishmin.srpos">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature android:name="android.hardware.camera" />

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_srpos"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_srpos"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:ignore="AllowBackup"
        tools:targetApi="m">
        <activity android:name=".settings.SettingsActivity" />
        <activity android:name=".activities.TryOutFile" />
        <activity android:name=".activities.StockActivity" />
        <activity android:name=".activities.ScannerActivity" />
        <activity android:name=".activities.Payment" />
        <activity
            android:name=".activities.VerifyOtpActivity"
            android:theme="@style/splashScreen" />
        <activity
            android:name=".activities.LoginActivity"
            android:theme="@style/splashScreen" />
        <activity
            android:name=".activities.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activities.SplashActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/app_name"
            android:theme="@style/splashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>